import React from "react";
import FormLogin from "@/components/FormLogin";
const loginPage = () => {
  return (
    <div className="zone-loginPage">
      <FormLogin />
    </div>
  );
};

export default loginPage;
